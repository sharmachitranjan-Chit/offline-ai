name: Android Build

# Builds a debug APK on every push/PR, and lets you trigger it manually.
# GitHub's runners have normal internet access to Google's Maven repo and
# Maven Central, so this is where the actual Android compile happens —
# it isn't something that can be done from a restricted sandbox.
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: Install JS dependencies
        run: npm ci

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Make gradlew executable
        run: chmod +x android/gradlew

      - name: Build debug APK
        working-directory: android
        run: ./gradlew assembleDebug --no-daemon

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: OfflineAI-debug-apk
          path: android/app/build/outputs/apk/debug/*.apk
          if-no-files-found: error
